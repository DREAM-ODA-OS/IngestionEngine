Puropose of test:
  In a standalone mode, verify the updateQualityMD operation on the interface IF-DREAM-O-UpdateQualityMD.

Prepare:
  Ensure the Ingestion Engine is running.  The download manager need not be running.
  The ODA server should be running.

Procedure:
  Execute the python script 'test_uqmd.py'

Evaluation:
  The test program reports results on stdout.
  Inspect the log of the Ingestion Engine.
  The updateQualityMD should be shown as having run successfully for the 'test valid input'.
  There are also some error conditions tested, the log should show an approprite error
  for each of the error condition tests.

