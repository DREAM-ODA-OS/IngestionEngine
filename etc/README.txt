Startup script, usually placed in /etc/init.d, but can
be elsewhere if need be.
Does not have a config file; the variable DREAM_IE_HOME 
must be configured in the 'dreamied' script itself.

Relies on the script 'dreamie' which must be placed
in DREAM_IE_HOME.
